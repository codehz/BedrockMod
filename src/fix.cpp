extern "C"
{
#include "stdio.h"
  static int __dso_handle;
  // FILE *stderr;

  // FILE *stdin;

  // FILE *stdout;

  // void _init()
  // {
  //   stdin = fdopen(0, "r");
  //   stdout = fdopen(1, "w");
  //   stderr = fdopen(2, "w");
  // }
}